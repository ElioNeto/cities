// Célula individual da grade (tile visual)

// Coordenadas na grade
grid_x = 0;
grid_y = 0;

// Tipo de célula que este objeto representa
cell_type = 0; // 0 = vazio

// Cor de debug (opcional, remover depois)
debug_color = c_white;